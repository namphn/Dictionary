package sample;import com.gtranslate.Language;import com.gtranslate.Translator;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.net.HttpURLConnection;import java.net.URL;import java.net.URLEncoder;import java.util.Scanner;public class GoogleAPI {    public static String Connect(String text) throws IOException {        String urlStr = " *** " + URLEncoder.encode(text, "UTF-8") + "&target=en&source=vi";    // URL translate API        //URL sẽ không được public        URL url = new URL(urlStr);        StringBuilder builder = new StringBuilder();        HttpURLConnection con = (HttpURLConnection) url.openConnection();           // kết nối với URL        con.setRequestProperty("User-Agent", "Mozilla/5.0");        Scanner scan = new Scanner(new InputStreamReader(con.getInputStream()));      // lấy input từ connection URL        String input;        while (scan.hasNext()) {            input = scan.nextLine();            builder.append(input);        }        String []spilit = builder.toString().split("\"");        return spilit[1];    }}